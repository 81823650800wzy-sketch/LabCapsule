# LabCapsule V1.3.0 测试报告

测试日期：2026-09-02（Asia/Shanghai）

## 自动化与构建

- Python/静态回归：77/77 通过。
- Android：aapt2、javac、D8、zipalign、apksigner 全流程通过。
- APK 元数据：包名 `com.labcapsule.remote`、`versionCode=130`、`versionName=1.3.0`、`minSdk=26`、`targetSdk=35`。
- APK 签名：v2 与 v3 验证通过，单一开发签名者。
- ESP-IDF：V5.5.4 全量编译通过；固件大小 `0x155b20`，3 MiB 应用分区剩余 56%。
- PyInstaller：V6.21.0 单文件 EXE 构建通过；隐藏窗口启动超过 8 秒未退出，随后仅终止本次测试进程。
- ADB：测试时没有连接 Android 设备，因此未执行真机 APK 安装；安装仍需用户在手机确认。

## COM8 实机

设备：ESP32-S3 rev 0.2、8 MiB PSRAM、MAC `00:00:00:00:00:00`、稳定设备 ID `lc-000000000000`。

1. 烧录：COM8 以 460800 baud 写入 bootloader、分区表、OTA 初始数据和应用；每个区域 SHA 校验通过。没有擦除 NVS、wallpaper、offline 分区。
2. 连接：`PING` 返回 `PONG,LABCAPSULE,1.0.0-alpha,DEVICE=lc-000000000000`。
3. 传感器：`SENSORS` 返回 `COUNT=1,MPU=OK,ADDRESS=0x68,RESULT=ESP_OK`。
4. 完整实验：`START,50,2` 返回 START 确认；共收到 100 点，最后返回 `OK,COMPLETE,SAMPLES=100`。
5. 终止实验：`START,50,5` 后在第 20 点发送 ABORT，返回 `OK,ABORT,SAMPLES=20`。
6. 静止样本示例：首点 `AX=0.0066 g, AY=-0.0039 g, AZ=1.0251 g`；末点时间戳约 1.989704 秒。

首次实测暴露了采样第 3 点可能出现 `ESP_ERR_INVALID_STATE` 的 I²C 竞争。修复内容为 MPU 与传感器扫描共享总线互斥、实验期间禁止主动扫描、单次读取重试、有限句柄恢复，以及最终失败时封存离线会话。重新编译和烧录后的 100 点完整实验不再复现。

启动日志中的 `WHO_AM_I=0x70` 表明当前模块可能是 MPU6500 兼容器件，而不是标准 ID 为 0x68 的原版 MPU6050；当前固件按兼容寄存器和已配置量程成功读取。若用于定量实验，仍建议执行六轴标定并确认具体芯片型号。

## 真实 Claude 联网参考

- 通过 `ClaudeBridge.research()` 实际调用本机 Claude。
- 仅传入工具 `WebSearch,WebFetch`，最大 4 轮、120 秒超时；未开放 Bash、Read、Write、Edit、MCP 或 Chrome。
- 28.16 秒成功返回 Espressif 官方产品页与数据手册 URL，并确认 ESP32-S3 仅支持 2.4 GHz 802.11 b/g/n。

## 发布产物

```text
LabCapsule-1.3.0.apk
size    401973
sha256  49ED81C2B17ABD4EBF49D2BB61087202F1624BE210866A60953C115CCAC9781E

LabCapsule-Studio-1.3.0.exe
size    85878312
sha256  04510A4E7AA240B4A75EE2FC77A7166F14EF1D05346B91DB3B71AC9357804CEA

labcapsule_firmware.bin
size    1399584
sha256  D378A96845C51CDD1E87689408A28F288728597A0087F671CAE0B05AF404307D
```

商业发行前仍需把 APK 开发签名替换为正式密钥，并为 Windows EXE 增加 Authenticode 代码签名。
