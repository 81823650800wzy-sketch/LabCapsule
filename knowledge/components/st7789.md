# ST7789 240×320 显示

SPI：SCK GPIO12、MOSI GPIO11、CS GPIO10、DC GPIO7、RST GPIO6、BL GPIO5。

设备端使用 RGB565 与双缓冲；动图媒体使用主机预处理后的 RGB332/RLE/差分帧。Live2D `.moc3` 必须在电脑或手机 GPU 上渲染，ESP 只播放由同一角色包生成的代理帧。这样角色相同，同时避免单帧持续传输与黑屏。
