# 连接策略

1. USB 有线：首次连接和固件维护的首选，延迟最低。
2. 局域网 HTTP：ESP32-S3 Station 仅支持 2.4 GHz；手机不需要连接设备的无网络热点。
3. BLE GATT：可配置外部 Wi‑Fi、控制设备、同步状态和低速实验数据，不影响手机互联网。
4. MQTT：设备主动连接 TLS Broker，用于跨网络远程状态与命令。

会话根据同一个 `deviceId` 合并，不因传输方式切换而创建新用户。
